## About
A set of scripts to save clipboard texts to a file and retrieve them using rofi.

## Usage

Create a shortcut to execute these files, then

Save the clipboard with

```bash
./save.bash
```

Retrieve a clipboard with

```bash
./pick.bash
```

And clean the clipboards with

```bash
./clean.bash
```
